# Switch to TDD workflow mode

Then use the TDD workflow defined in @library/practices/workflows/tdd-workflow-test-first-quality-gates.md to work on $ARGUMENT and use subagent to parallelize work when it is possible.
EOF < /dev/null