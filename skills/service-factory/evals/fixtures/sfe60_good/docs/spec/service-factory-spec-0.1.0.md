# Service Factory spec (fixture)

```
spec_version: 0.1.0
status: cemented baseline
```

A clean, self-contained spec body. Cross-references stay local: see `templates/rca.md`,
`gates/stamp_check.py`, `docs/spec/threat-model.md`. No external tether.
